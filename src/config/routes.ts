const routes = {
  home: "/",
  login: "/login",
  signup: "/signup",
  product: "/products",
  contact: "/contact",
  live: "/live",
  stream: "/live/stream",
  'produc-detail': "/products/:id",
  "page-not-found": "*",
};

export default routes;
